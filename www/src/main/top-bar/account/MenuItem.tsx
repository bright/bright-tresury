import MaterialMenuItem from '@material-ui/core/MenuItem'
import styled from '@material-ui/core/styles/styled'

const MenuItem = styled(MaterialMenuItem)(({ theme }) => ({
    padding: '14px 24px',
}))

export default MenuItem
