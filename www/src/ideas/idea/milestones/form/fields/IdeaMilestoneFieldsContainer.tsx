import styled from '@material-ui/core/styles/styled'

const IdeaMilestoneFieldsContainer = styled('div')(() => ({
    display: 'flex',
    flexDirection: 'column',
    gap: '2em',
    marginTop: '1em',
}))

export default IdeaMilestoneFieldsContainer
