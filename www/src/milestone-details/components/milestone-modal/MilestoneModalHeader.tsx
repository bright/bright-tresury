import styled from '@material-ui/core/styles/styled'

export const MilestoneModalHeader = styled('div')(() => ({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
}))
