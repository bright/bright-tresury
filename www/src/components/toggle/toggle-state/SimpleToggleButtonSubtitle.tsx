import { styled } from '@material-ui/core'
import React from 'react'

export const SimpleToggleButtonSubtitle = styled('div')(({ theme }) => ({
    fontStyle: 'italic',
    fontWeight: 'normal',
    fontSize: '14px',
}))

export default SimpleToggleButtonSubtitle
