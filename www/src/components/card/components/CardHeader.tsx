import { styled } from '@material-ui/core'

export default styled('div')({
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'flex-end',
    marginTop: '20px',
    marginBottom: '6px',
})
